services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: folk
      POSTGRES_PASSWORD: folk
      POSTGRES_DB: folk_medicine
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
